/**
 * 사이드바·탑바 로그아웃 트리거 공통 placeholder.
 *
 * 현재는 비동작 stub이며, Task 013(Phase 3)에서 logoutAction Server Action
 * 호출로 일괄 교체된다. sidebar/topbar 두 호출처를 단일 진실 공급원으로
 * 통합하여 교체 시점의 동기화 누락 위험을 제거한다.
 */
export function handleLogout(): void {
  // TODO(Task 013): logoutAction() Server Action 호출로 교체
}
